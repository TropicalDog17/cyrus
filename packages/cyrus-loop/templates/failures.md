# Failure rules — <repo>
<!--
One line per rule. Appended by learn.py when a diff-gate finding is tagged "recurring".
Loaded by Context (stage 3) into every run for this repo, so the executor stops
repeating past mistakes. Cap ~40 rules; consolidate past that (merge dupes, promote
stable ones into AGENTS.md, retire stale). Work-repo and personal libraries NEVER mix.

Format:  - R<n> (<YYYY-MM-DD>, <ISSUE-ID>): <imperative rule, one line>
-->
